import {notFound} from 'next/navigation';import {setRequestLocale} from 'next-intl/server';import {SolutionPage} from '@/components/marketing/solution-page';import {isSupportedLocale} from '@/i18n/routing';
export default async function Page({params}: {params: Promise<{locale: string}>}) {const {locale}=await params;if(!isSupportedLocale(locale))notFound();setRequestLocale(locale);return <SolutionPage locale={locale} titleVi="Kho cho doanh nghiệp nhỏ" titleEn="Storage for small businesses" bodyVi="Có thêm chỗ cho hàng mẫu, thiết bị và tồn kho mà không cần thuê thêm một văn phòng lớn." bodyEn="Create room for samples, equipment and inventory without taking on another large office lease."/>;}
