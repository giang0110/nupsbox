import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {setRequestLocale} from 'next-intl/server';import {Container} from '@/components/ui/container';import {Link} from '@/i18n/navigation';import {buttonClassName} from '@/components/ui/button';import {isSupportedLocale} from '@/i18n/routing';
export const metadata:Metadata={robots:{index:false,follow:true}};
export default async function Page({params}: {params: Promise<{locale:string}>}){const{locale}=await params;if(!isSupportedLocale(locale))notFound();setRequestLocale(locale);const vi=locale==='vi';return <main><section className="py-24"><Container><h1 className="max-w-3xl text-5xl font-black tracking-[-0.055em] text-[var(--nupsbox-navy)] sm:text-6xl">{vi?'Đặt kho online đang được chuẩn bị.':'Online booking is being prepared.'}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--nupsbox-slate)]">{vi?'Phase 1 chưa giả lập giữ chỗ hoặc tình trạng phòng theo thời gian thực. Hãy gửi nhu cầu để NupsBox xác nhận kho phù hợp.':'Phase 1 does not simulate real-time holds or unit availability. Send your requirements and NupsBox will confirm a suitable option.'}</p><Link href="/lien-he" className={buttonClassName({size:'lg',className:'mt-8'})}>{vi?'Nhận tư vấn':'Request advice'}</Link></Container></section></main>;}
