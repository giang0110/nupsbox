import {notFound} from 'next/navigation';import {setRequestLocale} from 'next-intl/server';import {HowItWorks} from '@/components/marketing/how-it-works';import {FinalCta} from '@/components/marketing/final-cta';import {Container} from '@/components/ui/container';import {isSupportedLocale} from '@/i18n/routing';
export default async function Page({params}: {params: Promise<{locale:string}>}){const{locale}=await params;if(!isSupportedLocale(locale))notFound();setRequestLocale(locale);const vi=locale==='vi';return <main><section className="bg-[var(--nupsbox-navy)] py-20 text-white"><Container><h1 className="text-5xl font-black tracking-[-0.055em] sm:text-6xl">{vi?'Cách thuê kho NupsBox':'How to rent at NupsBox'}</h1></Container></section><HowItWorks locale={locale}/><FinalCta locale={locale}/></main>;}
